<ul>
  {{#each model as |list|}}
    {{trello-list
      list=list
      modalActive=modalActive
      newItem=newItem
      selectItem='selectItem'
      createItem='createItem'}}
  {{/each}}

  {{create-list newList=model createList='createList'}}

</ul>

{{#if modalActive}}

  {{details-view
    selectedItem=selectedItem
    newItem=newItem
    toggleModal='toggleModal'
    deleteItem='deleteItem'}}

{{/if}}